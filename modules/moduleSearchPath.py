import sys

#this one returns the list of directories that python will look at to find a module 
sys.path

#so when python see an import-statement it will search all directories that is returned on the code line 4
# as soon as it findes the module, the search stops 